# ChurchTools Inventory Extension – Environment Template
#
# Quickstart
# 1. Copy this file to `.env`
# 2. Replace placeholder values with your instance credentials
# 3. Restart `npm run dev` so Vite picks up changes

# -----------------------------------------------------------------------------
# Core ChurchTools configuration
# -----------------------------------------------------------------------------

# Full base URL of your ChurchTools instance (must include protocol and subdomain)
# Example: https://mein-gemeinde.church.tools
VITE_BASE_URL=https://your-instance.church.tools

# Module key exposed by ChurchTools for this extension. Find it under
# Administration → Modules → Inventory Extension → Module key.
# Use `fkoinventorymanagement` for local/demo setups unless your admin provided
# a different value.
VITE_KEY=fkoinventorymanagement

# -----------------------------------------------------------------------------
# Development credentials (local usage only)
# ⚠️ SECURITY: Never commit live credentials to version control.
# Create a dedicated ChurchTools demo user with limited permissions.
# -----------------------------------------------------------------------------
VITE_USERNAME=your-dev-username
VITE_PASSWORD=your-dev-password

# -----------------------------------------------------------------------------
# Optional: API configuration overrides
# Adjust only when throttled by ChurchTools rate limits.
# -----------------------------------------------------------------------------
VITE_API_RATE_LIMIT=60